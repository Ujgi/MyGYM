@model IEnumerable<MyGYM.Models.Antrenor>

@{
    ViewData["Title"] = "Antrenör Listesi";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>🏋️‍♂️ Antrenör Kadrosu</h2>
       @if (User.IsInRole("Admin"))
        {
            <a asp-action="Create" class="btn btn-success">
                + Yeni Antrenör Ekle
            </a>
        }
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-info">
            Henüz sisteme kayıtlı bir antrenör yok. Sağ üstteki butondan ekleyebilirsiniz.
        </div>
    }
    else
    {
        <div class="card shadow-sm">
            <div class="card-body p-0">
                <table class="table table-striped table-hover mb-0">
                    <thead class="table-dark">
                        <tr>
                            <th>Ad Soyad</th>
                            <th>Uzmanlık Alanı</th>
                            <th class="text-end"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="text-white">@item.FullName</td>
                                <td><span class="badge bg-info text-dark">@item.Expertise</span></td>

                                <td class="text-end">
                                    @if (User.IsInRole("Admin"))
                                    {
                                        <div class="btn-group" role="group">
                                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Düzenle</a>
                                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Sil</a>
                                        </div>
                                    }

                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
</div>